'use server';

import { FC } from 'react';

const Home: FC = async () => (
  <main className='flex min-h-screen flex-col  p-8'>
    <h1 className='text-3xl mb-2'>Публичные страницы</h1>
  </main>
);

export default Home;
